server {
  listen 80 default_server;
  location / {
    proxy_pass http://172.16.22.5:9001;
    proxy_set_header Host	   $host;
    proxy_set_header X-Real-IP $remote_addr;
    client_max_body_size 3m;
  }
}
